<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SHORT_SERVICE"/>
    <uses-permission android:name="android.permission.REORDER_TASKS"/>
    <!-- Wake screen on disconnect notification -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <!-- Required for AlarmManager.setExactAndAllowWhileIdle on boot -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Declare this is a TV app so it appears in the TV launcher -->
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>

    <application
        android:label="flutter_app"
        android:name=".TvApplication"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:banner="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <!-- Disable Impeller — TV boxes crash with ImageReader/Impeller.
                 Uses stable Skia/GL renderer instead. -->
            <meta-data
                android:name="io.flutter.embedding.android.EnableImpeller"
                android:value="false"/>

            <!-- Standard phone/tablet launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Android TV launcher — makes the app appear on the TV home screen -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>

            <!-- Register as a Home app so user can set this as default launcher.
                 When set as default, Android launches this app automatically on boot
                 instead of the stock TV launcher. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- BootContentProvider: auto-initializes the app process on boot.
             ContentProviders are started by the framework before Application.onCreate()
             and are more reliable than BroadcastReceivers on many TV boxes. -->
        <provider
            android:name=".BootContentProvider"
            android:authorities="${applicationId}.bootprovider"
            android:exported="false"
            android:enabled="true"
            android:directBootAware="true"/>

        <!-- LaunchAlarmReceiver: AlarmManager backup for boot auto-start. -->
        <receiver
            android:name=".LaunchAlarmReceiver"
            android:exported="false"/>

        <!-- Boot receiver: starts the app automatically when the TV box powers on -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true"
            android:directBootAware="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="android.intent.action.REBOOT"/>
                <action android:name="android.intent.action.USER_PRESENT"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </receiver>

        <!-- DisconnectReceiver: launched by Flutter background isolate to
             force-open the app when a notification+data payload arrives -->
        <receiver
            android:name=".DisconnectReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.cct.appfortv.flutter_app.DISCONNECT"/>
            </intent-filter>
        </receiver>

        <!-- FCM service: receives push notifications -->
        <service
            android:name=".FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <!-- ForceOpenService: foreground service that launches MainActivity.
             Foreground services are permitted to call startActivity() on
             Android 10+, which plain background services cannot do. -->
        <service
            android:name=".ForceOpenService"
            android:exported="false"
            android:foregroundServiceType="shortService"
            tools:targetApi="34"/>


        <!-- Flutter embedding metadata -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
